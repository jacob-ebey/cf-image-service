{
  "name": "cf-image-service",
  "version": "0.0.0",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "start": "wrangler dev --local --persist",
    "deploy": "wrangler publish",
    "deploy:preview": "wrangler publish --env preview"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20221111.1",
    "type-fest": "^3.3.0",
    "typescript": "^4.9.3",
    "wrangler": "^2.6.1"
  },
  "dependencies": {
    "image-js": "^0.35.2",
    "preact": "^10.11.3",
    "preact-render-to-string": "^5.2.6",
    "remix-router-cf-worker": "0.0.7"
  }
}
